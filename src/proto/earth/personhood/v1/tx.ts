// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v2.12.1
//   protoc               unknown
// source: earth/personhood/v1/tx.proto

/* eslint-disable */
import { BinaryReader, BinaryWriter } from "@bufbuild/protobuf/wire";
import { Params } from "./params";

export const protobufPackage = "earth.personhood.v1";

/** MsgUpdateParams is the Msg/UpdateParams request type. */
export interface MsgUpdateParams {
  /** authority is the address that controls the module (defaults to x/gov unless overwritten). */
  authority: string;
  /** NOTE: All parameters must be supplied. */
  params: Params | undefined;
}

/**
 * MsgUpdateParamsResponse defines the response structure for executing a
 * MsgUpdateParams message.
 */
export interface MsgUpdateParamsResponse {
}

/**
 * MsgRegister registers the signer as a unique human. proof is a Barretenberg
 * UltraHonk (bb v5.0.0) proof and public_signals are its public signals as decimal
 * strings; their positions (nullifier/dsc_key/current_date) are governance params.
 * For the lean_poa circuit the order is [dsc_key, current_date, nullifier].
 * signature_algorithm selects the verifying key. The chain verifies the proof,
 * verifies dsc_der against the CSCA trust store and binds it to the proof's
 * dsc_key, pins current_date to block time, and dedups on the nullifier.
 */
export interface MsgRegister {
  creator: string;
  /** proof is the Barretenberg UltraHonk proof bytes (bb v5.0.0, poseidon2 flavor). */
  proof: Uint8Array;
  /**
   * public_signals are the circuit public signals as decimal strings; positions
   * are governance-configured (nullifier_index / dsc_key_index / current_date_index).
   */
  publicSignals: string[];
  /** affiliate is the optional referrer address (must be a registered human). */
  affiliate: string;
  /** signature_algorithm selects the verifying key (e.g. "rsa_65537_sha256"). */
  signatureAlgorithm: string;
  /**
   * dsc_der is the DER-encoded Document Signer certificate that signed the
   * passport's SOD. The chain verifies it chains to a trusted CSCA and is not
   * revoked, then recomputes its commitment and requires the proof's public
   * input to match — so the proof is bound to this specific signer.
   */
  dscDer: Uint8Array;
}

/** MsgRegisterResponse returns the registration reward paid to the registree. */
export interface MsgRegisterResponse {
  reward: string;
}

/** MsgClaimAnml defines the MsgClaimAnml message. */
export interface MsgClaimAnml {
  creator: string;
}

/** MsgClaimAnmlResponse defines the MsgClaimAnmlResponse message. */
export interface MsgClaimAnmlResponse {
}

function createBaseMsgUpdateParams(): MsgUpdateParams {
  return { authority: "", params: undefined };
}

export const MsgUpdateParams: MessageFns<MsgUpdateParams> = {
  encode(message: MsgUpdateParams, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.authority !== "") {
      writer.uint32(10).string(message.authority);
    }
    if (message.params !== undefined) {
      Params.encode(message.params, writer.uint32(18).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): MsgUpdateParams {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const previousRecursionDepth = (reader as any).__tsProtoDecodeDepth ?? 0;
    if (previousRecursionDepth >= 100) {
      throw new globalThis.Error("protobuf decode recursion limit exceeded");
    }
    (reader as any).__tsProtoDecodeDepth = previousRecursionDepth + 1;
    try {
      const end = length === undefined ? reader.len : reader.pos + length;
      const message = createBaseMsgUpdateParams();
      while (reader.pos < end) {
        const tag = reader.uint32();
        switch (tag >>> 3) {
          case 1: {
            if (tag !== 10) {
              break;
            }

            message.authority = reader.string();
            continue;
          }
          case 2: {
            if (tag !== 18) {
              break;
            }

            message.params = Params.decode(reader, reader.uint32());
            continue;
          }
        }
        if ((tag & 7) === 4 || tag === 0) {
          break;
        }
        reader.skip(tag & 7);
      }
      return message;
    } finally {
      (reader as any).__tsProtoDecodeDepth = previousRecursionDepth;
    }
  },

  fromJSON(object: any): MsgUpdateParams {
    return {
      authority: isSet(object.authority) ? globalThis.String(object.authority) : "",
      params: isSet(object.params) ? Params.fromJSON(object.params) : undefined,
    };
  },

  toJSON(message: MsgUpdateParams): unknown {
    const obj: any = {};
    if (message.authority !== "") {
      obj.authority = message.authority;
    }
    if (message.params !== undefined) {
      obj.params = Params.toJSON(message.params);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<MsgUpdateParams>, I>>(base?: I): MsgUpdateParams {
    return MsgUpdateParams.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<MsgUpdateParams>, I>>(object: I): MsgUpdateParams {
    const message = createBaseMsgUpdateParams();
    message.authority = object.authority ?? "";
    message.params = (object.params !== undefined && object.params !== null)
      ? Params.fromPartial(object.params)
      : undefined;
    return message;
  },
};

function createBaseMsgUpdateParamsResponse(): MsgUpdateParamsResponse {
  return {};
}

export const MsgUpdateParamsResponse: MessageFns<MsgUpdateParamsResponse> = {
  encode(_: MsgUpdateParamsResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): MsgUpdateParamsResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const previousRecursionDepth = (reader as any).__tsProtoDecodeDepth ?? 0;
    if (previousRecursionDepth >= 100) {
      throw new globalThis.Error("protobuf decode recursion limit exceeded");
    }
    (reader as any).__tsProtoDecodeDepth = previousRecursionDepth + 1;
    try {
      const end = length === undefined ? reader.len : reader.pos + length;
      const message = createBaseMsgUpdateParamsResponse();
      while (reader.pos < end) {
        const tag = reader.uint32();
        switch (tag >>> 3) {
        }
        if ((tag & 7) === 4 || tag === 0) {
          break;
        }
        reader.skip(tag & 7);
      }
      return message;
    } finally {
      (reader as any).__tsProtoDecodeDepth = previousRecursionDepth;
    }
  },

  fromJSON(_: any): MsgUpdateParamsResponse {
    return {};
  },

  toJSON(_: MsgUpdateParamsResponse): unknown {
    const obj: any = {};
    return obj;
  },

  create<I extends Exact<DeepPartial<MsgUpdateParamsResponse>, I>>(base?: I): MsgUpdateParamsResponse {
    return MsgUpdateParamsResponse.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<MsgUpdateParamsResponse>, I>>(_: I): MsgUpdateParamsResponse {
    const message = createBaseMsgUpdateParamsResponse();
    return message;
  },
};

function createBaseMsgRegister(): MsgRegister {
  return {
    creator: "",
    proof: new Uint8Array(0),
    publicSignals: [],
    affiliate: "",
    signatureAlgorithm: "",
    dscDer: new Uint8Array(0),
  };
}

export const MsgRegister: MessageFns<MsgRegister> = {
  encode(message: MsgRegister, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.creator !== "") {
      writer.uint32(10).string(message.creator);
    }
    if (message.proof.length !== 0) {
      writer.uint32(18).bytes(message.proof);
    }
    for (const v of message.publicSignals) {
      writer.uint32(26).string(v!);
    }
    if (message.affiliate !== "") {
      writer.uint32(34).string(message.affiliate);
    }
    if (message.signatureAlgorithm !== "") {
      writer.uint32(42).string(message.signatureAlgorithm);
    }
    if (message.dscDer.length !== 0) {
      writer.uint32(50).bytes(message.dscDer);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): MsgRegister {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const previousRecursionDepth = (reader as any).__tsProtoDecodeDepth ?? 0;
    if (previousRecursionDepth >= 100) {
      throw new globalThis.Error("protobuf decode recursion limit exceeded");
    }
    (reader as any).__tsProtoDecodeDepth = previousRecursionDepth + 1;
    try {
      const end = length === undefined ? reader.len : reader.pos + length;
      const message = createBaseMsgRegister();
      while (reader.pos < end) {
        const tag = reader.uint32();
        switch (tag >>> 3) {
          case 1: {
            if (tag !== 10) {
              break;
            }

            message.creator = reader.string();
            continue;
          }
          case 2: {
            if (tag !== 18) {
              break;
            }

            message.proof = reader.bytes();
            continue;
          }
          case 3: {
            if (tag !== 26) {
              break;
            }

            message.publicSignals.push(reader.string());
            continue;
          }
          case 4: {
            if (tag !== 34) {
              break;
            }

            message.affiliate = reader.string();
            continue;
          }
          case 5: {
            if (tag !== 42) {
              break;
            }

            message.signatureAlgorithm = reader.string();
            continue;
          }
          case 6: {
            if (tag !== 50) {
              break;
            }

            message.dscDer = reader.bytes();
            continue;
          }
        }
        if ((tag & 7) === 4 || tag === 0) {
          break;
        }
        reader.skip(tag & 7);
      }
      return message;
    } finally {
      (reader as any).__tsProtoDecodeDepth = previousRecursionDepth;
    }
  },

  fromJSON(object: any): MsgRegister {
    return {
      creator: isSet(object.creator) ? globalThis.String(object.creator) : "",
      proof: isSet(object.proof) ? bytesFromBase64(object.proof) : new Uint8Array(0),
      publicSignals: globalThis.Array.isArray(object?.publicSignals)
        ? object.publicSignals.map((e: any) => globalThis.String(e))
        : globalThis.Array.isArray(object?.public_signals)
        ? object.public_signals.map((e: any) => globalThis.String(e))
        : [],
      affiliate: isSet(object.affiliate) ? globalThis.String(object.affiliate) : "",
      signatureAlgorithm: isSet(object.signatureAlgorithm)
        ? globalThis.String(object.signatureAlgorithm)
        : isSet(object.signature_algorithm)
        ? globalThis.String(object.signature_algorithm)
        : "",
      dscDer: isSet(object.dscDer)
        ? bytesFromBase64(object.dscDer)
        : isSet(object.dsc_der)
        ? bytesFromBase64(object.dsc_der)
        : new Uint8Array(0),
    };
  },

  toJSON(message: MsgRegister): unknown {
    const obj: any = {};
    if (message.creator !== "") {
      obj.creator = message.creator;
    }
    if (message.proof.length !== 0) {
      obj.proof = base64FromBytes(message.proof);
    }
    if (message.publicSignals?.length) {
      obj.publicSignals = message.publicSignals;
    }
    if (message.affiliate !== "") {
      obj.affiliate = message.affiliate;
    }
    if (message.signatureAlgorithm !== "") {
      obj.signatureAlgorithm = message.signatureAlgorithm;
    }
    if (message.dscDer.length !== 0) {
      obj.dscDer = base64FromBytes(message.dscDer);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<MsgRegister>, I>>(base?: I): MsgRegister {
    return MsgRegister.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<MsgRegister>, I>>(object: I): MsgRegister {
    const message = createBaseMsgRegister();
    message.creator = object.creator ?? "";
    message.proof = object.proof ?? new Uint8Array(0);
    message.publicSignals = object.publicSignals?.map((e) => e) || [];
    message.affiliate = object.affiliate ?? "";
    message.signatureAlgorithm = object.signatureAlgorithm ?? "";
    message.dscDer = object.dscDer ?? new Uint8Array(0);
    return message;
  },
};

function createBaseMsgRegisterResponse(): MsgRegisterResponse {
  return { reward: "" };
}

export const MsgRegisterResponse: MessageFns<MsgRegisterResponse> = {
  encode(message: MsgRegisterResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.reward !== "") {
      writer.uint32(10).string(message.reward);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): MsgRegisterResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const previousRecursionDepth = (reader as any).__tsProtoDecodeDepth ?? 0;
    if (previousRecursionDepth >= 100) {
      throw new globalThis.Error("protobuf decode recursion limit exceeded");
    }
    (reader as any).__tsProtoDecodeDepth = previousRecursionDepth + 1;
    try {
      const end = length === undefined ? reader.len : reader.pos + length;
      const message = createBaseMsgRegisterResponse();
      while (reader.pos < end) {
        const tag = reader.uint32();
        switch (tag >>> 3) {
          case 1: {
            if (tag !== 10) {
              break;
            }

            message.reward = reader.string();
            continue;
          }
        }
        if ((tag & 7) === 4 || tag === 0) {
          break;
        }
        reader.skip(tag & 7);
      }
      return message;
    } finally {
      (reader as any).__tsProtoDecodeDepth = previousRecursionDepth;
    }
  },

  fromJSON(object: any): MsgRegisterResponse {
    return { reward: isSet(object.reward) ? globalThis.String(object.reward) : "" };
  },

  toJSON(message: MsgRegisterResponse): unknown {
    const obj: any = {};
    if (message.reward !== "") {
      obj.reward = message.reward;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<MsgRegisterResponse>, I>>(base?: I): MsgRegisterResponse {
    return MsgRegisterResponse.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<MsgRegisterResponse>, I>>(object: I): MsgRegisterResponse {
    const message = createBaseMsgRegisterResponse();
    message.reward = object.reward ?? "";
    return message;
  },
};

function createBaseMsgClaimAnml(): MsgClaimAnml {
  return { creator: "" };
}

export const MsgClaimAnml: MessageFns<MsgClaimAnml> = {
  encode(message: MsgClaimAnml, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.creator !== "") {
      writer.uint32(10).string(message.creator);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): MsgClaimAnml {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const previousRecursionDepth = (reader as any).__tsProtoDecodeDepth ?? 0;
    if (previousRecursionDepth >= 100) {
      throw new globalThis.Error("protobuf decode recursion limit exceeded");
    }
    (reader as any).__tsProtoDecodeDepth = previousRecursionDepth + 1;
    try {
      const end = length === undefined ? reader.len : reader.pos + length;
      const message = createBaseMsgClaimAnml();
      while (reader.pos < end) {
        const tag = reader.uint32();
        switch (tag >>> 3) {
          case 1: {
            if (tag !== 10) {
              break;
            }

            message.creator = reader.string();
            continue;
          }
        }
        if ((tag & 7) === 4 || tag === 0) {
          break;
        }
        reader.skip(tag & 7);
      }
      return message;
    } finally {
      (reader as any).__tsProtoDecodeDepth = previousRecursionDepth;
    }
  },

  fromJSON(object: any): MsgClaimAnml {
    return { creator: isSet(object.creator) ? globalThis.String(object.creator) : "" };
  },

  toJSON(message: MsgClaimAnml): unknown {
    const obj: any = {};
    if (message.creator !== "") {
      obj.creator = message.creator;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<MsgClaimAnml>, I>>(base?: I): MsgClaimAnml {
    return MsgClaimAnml.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<MsgClaimAnml>, I>>(object: I): MsgClaimAnml {
    const message = createBaseMsgClaimAnml();
    message.creator = object.creator ?? "";
    return message;
  },
};

function createBaseMsgClaimAnmlResponse(): MsgClaimAnmlResponse {
  return {};
}

export const MsgClaimAnmlResponse: MessageFns<MsgClaimAnmlResponse> = {
  encode(_: MsgClaimAnmlResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): MsgClaimAnmlResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const previousRecursionDepth = (reader as any).__tsProtoDecodeDepth ?? 0;
    if (previousRecursionDepth >= 100) {
      throw new globalThis.Error("protobuf decode recursion limit exceeded");
    }
    (reader as any).__tsProtoDecodeDepth = previousRecursionDepth + 1;
    try {
      const end = length === undefined ? reader.len : reader.pos + length;
      const message = createBaseMsgClaimAnmlResponse();
      while (reader.pos < end) {
        const tag = reader.uint32();
        switch (tag >>> 3) {
        }
        if ((tag & 7) === 4 || tag === 0) {
          break;
        }
        reader.skip(tag & 7);
      }
      return message;
    } finally {
      (reader as any).__tsProtoDecodeDepth = previousRecursionDepth;
    }
  },

  fromJSON(_: any): MsgClaimAnmlResponse {
    return {};
  },

  toJSON(_: MsgClaimAnmlResponse): unknown {
    const obj: any = {};
    return obj;
  },

  create<I extends Exact<DeepPartial<MsgClaimAnmlResponse>, I>>(base?: I): MsgClaimAnmlResponse {
    return MsgClaimAnmlResponse.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<MsgClaimAnmlResponse>, I>>(_: I): MsgClaimAnmlResponse {
    const message = createBaseMsgClaimAnmlResponse();
    return message;
  },
};

/** Msg defines the Msg service. */
export interface Msg {
  /**
   * UpdateParams defines a (governance) operation for updating the module
   * parameters. The authority defaults to the x/gov module account.
   */
  UpdateParams(request: MsgUpdateParams): Promise<MsgUpdateParamsResponse>;
  /** Register defines the Register RPC. */
  Register(request: MsgRegister): Promise<MsgRegisterResponse>;
  /** ClaimAnml defines the ClaimAnml RPC. */
  ClaimAnml(request: MsgClaimAnml): Promise<MsgClaimAnmlResponse>;
}

export const MsgServiceName = "earth.personhood.v1.Msg";
export class MsgClientImpl implements Msg {
  private readonly rpc: Rpc;
  private readonly service: string;
  constructor(rpc: Rpc, opts?: { service?: string }) {
    this.service = opts?.service || MsgServiceName;
    this.rpc = rpc;
    this.UpdateParams = this.UpdateParams.bind(this);
    this.Register = this.Register.bind(this);
    this.ClaimAnml = this.ClaimAnml.bind(this);
  }
  UpdateParams(request: MsgUpdateParams): Promise<MsgUpdateParamsResponse> {
    const data = MsgUpdateParams.encode(request).finish();
    const promise = this.rpc.request(this.service, "UpdateParams", data);
    return promise.then((data) => MsgUpdateParamsResponse.decode(new BinaryReader(data)));
  }

  Register(request: MsgRegister): Promise<MsgRegisterResponse> {
    const data = MsgRegister.encode(request).finish();
    const promise = this.rpc.request(this.service, "Register", data);
    return promise.then((data) => MsgRegisterResponse.decode(new BinaryReader(data)));
  }

  ClaimAnml(request: MsgClaimAnml): Promise<MsgClaimAnmlResponse> {
    const data = MsgClaimAnml.encode(request).finish();
    const promise = this.rpc.request(this.service, "ClaimAnml", data);
    return promise.then((data) => MsgClaimAnmlResponse.decode(new BinaryReader(data)));
  }
}

interface Rpc {
  request(service: string, method: string, data: Uint8Array): Promise<Uint8Array>;
}

function bytesFromBase64(b64: string): Uint8Array {
  if ((globalThis as any).Buffer) {
    return Uint8Array.from((globalThis as any).Buffer.from(b64, "base64"));
  } else {
    const bin = globalThis.atob(b64);
    const arr = new Uint8Array(bin.length);
    for (let i = 0; i < bin.length; ++i) {
      arr[i] = bin.charCodeAt(i);
    }
    return arr;
  }
}

function base64FromBytes(arr: Uint8Array): string {
  if ((globalThis as any).Buffer) {
    return (globalThis as any).Buffer.from(arr).toString("base64");
  } else {
    const bin: string[] = [];
    arr.forEach((byte) => {
      bin.push(globalThis.String.fromCharCode(byte));
    });
    return globalThis.btoa(bin.join(""));
  }
}

type Builtin = Date | Function | Uint8Array | string | number | boolean | undefined;

export type DeepPartial<T> = T extends Builtin ? T
  : T extends globalThis.Array<infer U> ? globalThis.Array<DeepPartial<U>>
  : T extends ReadonlyArray<infer U> ? ReadonlyArray<DeepPartial<U>>
  : T extends {} ? { [K in keyof T]?: DeepPartial<T[K]> }
  : Partial<T>;

type KeysOfUnion<T> = T extends T ? keyof T : never;
export type Exact<P, I extends P> = P extends Builtin ? P
  : P & { [K in keyof P]: Exact<P[K], I[K]> } & { [K in Exclude<keyof I, KeysOfUnion<P>>]: never };

function isSet(value: any): boolean {
  return value !== null && value !== undefined;
}

export interface MessageFns<T> {
  encode(message: T, writer?: BinaryWriter): BinaryWriter;
  decode(input: BinaryReader | Uint8Array, length?: number): T;
  fromJSON(object: any): T;
  toJSON(message: T): unknown;
  create<I extends Exact<DeepPartial<T>, I>>(base?: I): T;
  fromPartial<I extends Exact<DeepPartial<T>, I>>(object: I): T;
}
