// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v2.12.0
//   protoc               unknown
// source: earth/personhood/v1/registration.proto

/* eslint-disable */
import { BinaryReader, BinaryWriter } from "@bufbuild/protobuf/wire";

export const protobufPackage = "earth.personhood.v1";

/**
 * Registration is a proof-of-personhood record: one unique human, deduped by the
 * zk nullifier, bound to a wallet address.
 */
export interface Registration {
  /** nullifier is the unique per-person value output by the registration proof. */
  nullifier: Uint8Array;
  /** address is the registered wallet. */
  address: string;
  /** registered_at is the unix time (seconds) of registration. */
  registeredAt: number;
  /** last_anml_claim is the unix time (seconds, UTC midnight) of the last ANML claim. */
  lastAnmlClaim: number;
  /**
   * dsc_key is the Poseidon2 commitment to the Document Signer that produced the
   * registration proof — the same value the circuit exposed publicly. Recording
   * it is what makes a compromised signer's registrations enumerable.
   */
  dscKey: Uint8Array;
  /**
   * country is the DSC's ISO 3166-1 alpha-2 issuing country, or "" when the
   * certificate carries no country attribute.
   */
  country: string;
}

function createBaseRegistration(): Registration {
  return {
    nullifier: new Uint8Array(0),
    address: "",
    registeredAt: 0,
    lastAnmlClaim: 0,
    dscKey: new Uint8Array(0),
    country: "",
  };
}

export const Registration: MessageFns<Registration> = {
  encode(message: Registration, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.nullifier.length !== 0) {
      writer.uint32(10).bytes(message.nullifier);
    }
    if (message.address !== "") {
      writer.uint32(18).string(message.address);
    }
    if (message.registeredAt !== 0) {
      writer.uint32(24).int64(message.registeredAt);
    }
    if (message.lastAnmlClaim !== 0) {
      writer.uint32(32).int64(message.lastAnmlClaim);
    }
    if (message.dscKey.length !== 0) {
      writer.uint32(42).bytes(message.dscKey);
    }
    if (message.country !== "") {
      writer.uint32(50).string(message.country);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): Registration {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseRegistration();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.nullifier = reader.bytes();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.address = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 24) {
            break;
          }

          message.registeredAt = longToNumber(reader.int64());
          continue;
        }
        case 4: {
          if (tag !== 32) {
            break;
          }

          message.lastAnmlClaim = longToNumber(reader.int64());
          continue;
        }
        case 5: {
          if (tag !== 42) {
            break;
          }

          message.dscKey = reader.bytes();
          continue;
        }
        case 6: {
          if (tag !== 50) {
            break;
          }

          message.country = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): Registration {
    return {
      nullifier: isSet(object.nullifier) ? bytesFromBase64(object.nullifier) : new Uint8Array(0),
      address: isSet(object.address) ? globalThis.String(object.address) : "",
      registeredAt: isSet(object.registeredAt)
        ? globalThis.Number(object.registeredAt)
        : isSet(object.registered_at)
        ? globalThis.Number(object.registered_at)
        : 0,
      lastAnmlClaim: isSet(object.lastAnmlClaim)
        ? globalThis.Number(object.lastAnmlClaim)
        : isSet(object.last_anml_claim)
        ? globalThis.Number(object.last_anml_claim)
        : 0,
      dscKey: isSet(object.dscKey)
        ? bytesFromBase64(object.dscKey)
        : isSet(object.dsc_key)
        ? bytesFromBase64(object.dsc_key)
        : new Uint8Array(0),
      country: isSet(object.country) ? globalThis.String(object.country) : "",
    };
  },

  toJSON(message: Registration): unknown {
    const obj: any = {};
    if (message.nullifier.length !== 0) {
      obj.nullifier = base64FromBytes(message.nullifier);
    }
    if (message.address !== "") {
      obj.address = message.address;
    }
    if (message.registeredAt !== 0) {
      obj.registeredAt = Math.round(message.registeredAt);
    }
    if (message.lastAnmlClaim !== 0) {
      obj.lastAnmlClaim = Math.round(message.lastAnmlClaim);
    }
    if (message.dscKey.length !== 0) {
      obj.dscKey = base64FromBytes(message.dscKey);
    }
    if (message.country !== "") {
      obj.country = message.country;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<Registration>, I>>(base?: I): Registration {
    return Registration.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<Registration>, I>>(object: I): Registration {
    const message = createBaseRegistration();
    message.nullifier = object.nullifier ?? new Uint8Array(0);
    message.address = object.address ?? "";
    message.registeredAt = object.registeredAt ?? 0;
    message.lastAnmlClaim = object.lastAnmlClaim ?? 0;
    message.dscKey = object.dscKey ?? new Uint8Array(0);
    message.country = object.country ?? "";
    return message;
  },
};

function bytesFromBase64(b64: string): Uint8Array {
  if ((globalThis as any).Buffer) {
    return Uint8Array.from((globalThis as any).Buffer.from(b64, "base64"));
  } else {
    const bin = globalThis.atob(b64);
    const arr = new Uint8Array(bin.length);
    for (let i = 0; i < bin.length; ++i) {
      arr[i] = bin.charCodeAt(i);
    }
    return arr;
  }
}

function base64FromBytes(arr: Uint8Array): string {
  if ((globalThis as any).Buffer) {
    return (globalThis as any).Buffer.from(arr).toString("base64");
  } else {
    const bin: string[] = [];
    arr.forEach((byte) => {
      bin.push(globalThis.String.fromCharCode(byte));
    });
    return globalThis.btoa(bin.join(""));
  }
}

type Builtin = Date | Function | Uint8Array | string | number | boolean | undefined;

export type DeepPartial<T> = T extends Builtin ? T
  : T extends globalThis.Array<infer U> ? globalThis.Array<DeepPartial<U>>
  : T extends ReadonlyArray<infer U> ? ReadonlyArray<DeepPartial<U>>
  : T extends {} ? { [K in keyof T]?: DeepPartial<T[K]> }
  : Partial<T>;

type KeysOfUnion<T> = T extends T ? keyof T : never;
export type Exact<P, I extends P> = P extends Builtin ? P
  : P & { [K in keyof P]: Exact<P[K], I[K]> } & { [K in Exclude<keyof I, KeysOfUnion<P>>]: never };

function longToNumber(int64: { toString(): string }): number {
  const num = globalThis.Number(int64.toString());
  if (num > globalThis.Number.MAX_SAFE_INTEGER) {
    throw new globalThis.Error("Value is larger than Number.MAX_SAFE_INTEGER");
  }
  if (num < globalThis.Number.MIN_SAFE_INTEGER) {
    throw new globalThis.Error("Value is smaller than Number.MIN_SAFE_INTEGER");
  }
  return num;
}

function isSet(value: any): boolean {
  return value !== null && value !== undefined;
}

export interface MessageFns<T> {
  encode(message: T, writer?: BinaryWriter): BinaryWriter;
  decode(input: BinaryReader | Uint8Array, length?: number): T;
  fromJSON(object: any): T;
  toJSON(message: T): unknown;
  create<I extends Exact<DeepPartial<T>, I>>(base?: I): T;
  fromPartial<I extends Exact<DeepPartial<T>, I>>(object: I): T;
}
