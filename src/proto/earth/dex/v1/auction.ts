// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v2.12.1
//   protoc               unknown
// source: earth/dex/v1/auction.proto

/* eslint-disable */
import { BinaryReader, BinaryWriter } from "@bufbuild/protobuf/wire";
import { Coin } from "../../../cosmos/base/v1beta1/coin";

export const protobufPackage = "earth.dex.v1";

/** AuctionStatus is the lifecycle of the genesis liquidity auction. */
export enum AuctionStatus {
  AUCTION_STATUS_UNSPECIFIED = 0,
  /**
   * AUCTION_STATUS_PENDING - PENDING: the ERTH is pre-funded on the module account and earmarked, but no
   * bidding window is open. This is the state genesis starts in, and the state
   * an auction that drew no bids returns to, so governance can try again.
   */
  AUCTION_STATUS_PENDING = 1,
  /** AUCTION_STATUS_OPEN - OPEN: accepting bids in bid_denom until end_time. */
  AUCTION_STATUS_OPEN = 2,
  /** AUCTION_STATUS_SETTLED - SETTLED: the pool exists and bidders may claim. Terminal. */
  AUCTION_STATUS_SETTLED = 3,
  UNRECOGNIZED = -1,
}

export function auctionStatusFromJSON(object: any): AuctionStatus {
  switch (object) {
    case 0:
    case "AUCTION_STATUS_UNSPECIFIED":
      return AuctionStatus.AUCTION_STATUS_UNSPECIFIED;
    case 1:
    case "AUCTION_STATUS_PENDING":
      return AuctionStatus.AUCTION_STATUS_PENDING;
    case 2:
    case "AUCTION_STATUS_OPEN":
      return AuctionStatus.AUCTION_STATUS_OPEN;
    case 3:
    case "AUCTION_STATUS_SETTLED":
      return AuctionStatus.AUCTION_STATUS_SETTLED;
    case -1:
    case "UNRECOGNIZED":
    default:
      return AuctionStatus.UNRECOGNIZED;
  }
}

export function auctionStatusToJSON(object: AuctionStatus): string {
  switch (object) {
    case AuctionStatus.AUCTION_STATUS_UNSPECIFIED:
      return "AUCTION_STATUS_UNSPECIFIED";
    case AuctionStatus.AUCTION_STATUS_PENDING:
      return "AUCTION_STATUS_PENDING";
    case AuctionStatus.AUCTION_STATUS_OPEN:
      return "AUCTION_STATUS_OPEN";
    case AuctionStatus.AUCTION_STATUS_SETTLED:
      return "AUCTION_STATUS_SETTLED";
    case AuctionStatus.UNRECOGNIZED:
    default:
      return "UNRECOGNIZED";
  }
}

/**
 * LiquidityAuction is the one-shot genesis liquidity event.
 *
 * Two thirds of the pre-mine sits on the dex module account from genesis, split
 * in half: one half is paid out to bidders pro rata, the other half is paired
 * with everything they bid to open the pool. Because both halves are equal, the
 * pool opens at exactly the price the auction cleared at -- bidders paid
 * total_raised for erth_for_bidders, and the pool is seeded with the same ratio.
 * There is no separate price to set and no gap to arbitrage on the first block.
 *
 * The resulting LP shares are minted to the module account, which cannot sign a
 * transaction, so this liquidity is permanent in the same way the genesis
 * ANML/ERTH pool is. Nobody owns it, governance included.
 */
export interface LiquidityAuction {
  status: AuctionStatus;
  /**
   * bid_denom is the asset bids are denominated in, chosen by governance when
   * the window opens rather than fixed at genesis -- the intended denominator
   * is IBC USDC, which does not exist on the chain until IBC is enabled.
   */
  bidDenom: string;
  /**
   * end_time is the unix timestamp (seconds) at which bidding closes and the
   * pool is created. Settlement happens in the EndBlocker, not on a message, so
   * it cannot be withheld by anyone declining to send one.
   */
  endTime: number;
  /** erth_for_bidders is the earmark paid out to bidders pro rata to their bid. */
  erthForBidders:
    | Coin
    | undefined;
  /** erth_for_pool is the earmark paired with total_raised to open the pool. */
  erthForPool:
    | Coin
    | undefined;
  /** total_raised is the sum of every bid placed in the open window. */
  totalRaised: string;
  /** pool_id is the pool opened at settlement. Zero until then. */
  poolId: number;
  /**
   * claimed is the running sum of ERTH already claimed by bidders. It exists so
   * the last claimant can be paid the remainder rather than a truncated share:
   * pro-rata division floors, so the individual shares do not sum to the
   * earmark, and without this the dust would be stranded on the module account.
   */
  claimed: string;
}

/** AuctionBid is one bidder's total contribution to the open window. */
export interface AuctionBid {
  bidder: string;
  /**
   * amount is the cumulative bid in bid_denom. Bidding more than once adds to
   * it; there is no withdrawal, which is what makes total_raised final at
   * end_time.
   */
  amount: string;
  /**
   * claimed records that this bidder has taken their ERTH. The entry is kept
   * rather than deleted so a second claim fails loudly instead of looking like
   * a bidder who never participated.
   */
  claimed: boolean;
}

function createBaseLiquidityAuction(): LiquidityAuction {
  return {
    status: 0,
    bidDenom: "",
    endTime: 0,
    erthForBidders: undefined,
    erthForPool: undefined,
    totalRaised: "",
    poolId: 0,
    claimed: "",
  };
}

export const LiquidityAuction: MessageFns<LiquidityAuction> = {
  encode(message: LiquidityAuction, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.status !== 0) {
      writer.uint32(8).int32(message.status);
    }
    if (message.bidDenom !== "") {
      writer.uint32(18).string(message.bidDenom);
    }
    if (message.endTime !== 0) {
      writer.uint32(24).int64(message.endTime);
    }
    if (message.erthForBidders !== undefined) {
      Coin.encode(message.erthForBidders, writer.uint32(34).fork()).join();
    }
    if (message.erthForPool !== undefined) {
      Coin.encode(message.erthForPool, writer.uint32(42).fork()).join();
    }
    if (message.totalRaised !== "") {
      writer.uint32(50).string(message.totalRaised);
    }
    if (message.poolId !== 0) {
      writer.uint32(56).uint64(message.poolId);
    }
    if (message.claimed !== "") {
      writer.uint32(66).string(message.claimed);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): LiquidityAuction {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const previousRecursionDepth = (reader as any).__tsProtoDecodeDepth ?? 0;
    if (previousRecursionDepth >= 100) {
      throw new globalThis.Error("protobuf decode recursion limit exceeded");
    }
    (reader as any).__tsProtoDecodeDepth = previousRecursionDepth + 1;
    try {
      const end = length === undefined ? reader.len : reader.pos + length;
      const message = createBaseLiquidityAuction();
      while (reader.pos < end) {
        const tag = reader.uint32();
        switch (tag >>> 3) {
          case 1: {
            if (tag !== 8) {
              break;
            }

            message.status = reader.int32() as any;
            continue;
          }
          case 2: {
            if (tag !== 18) {
              break;
            }

            message.bidDenom = reader.string();
            continue;
          }
          case 3: {
            if (tag !== 24) {
              break;
            }

            message.endTime = longToNumber(reader.int64());
            continue;
          }
          case 4: {
            if (tag !== 34) {
              break;
            }

            message.erthForBidders = Coin.decode(reader, reader.uint32());
            continue;
          }
          case 5: {
            if (tag !== 42) {
              break;
            }

            message.erthForPool = Coin.decode(reader, reader.uint32());
            continue;
          }
          case 6: {
            if (tag !== 50) {
              break;
            }

            message.totalRaised = reader.string();
            continue;
          }
          case 7: {
            if (tag !== 56) {
              break;
            }

            message.poolId = longToNumber(reader.uint64());
            continue;
          }
          case 8: {
            if (tag !== 66) {
              break;
            }

            message.claimed = reader.string();
            continue;
          }
        }
        if ((tag & 7) === 4 || tag === 0) {
          break;
        }
        reader.skip(tag & 7);
      }
      return message;
    } finally {
      (reader as any).__tsProtoDecodeDepth = previousRecursionDepth;
    }
  },

  fromJSON(object: any): LiquidityAuction {
    return {
      status: isSet(object.status) ? auctionStatusFromJSON(object.status) : 0,
      bidDenom: isSet(object.bidDenom)
        ? globalThis.String(object.bidDenom)
        : isSet(object.bid_denom)
        ? globalThis.String(object.bid_denom)
        : "",
      endTime: isSet(object.endTime)
        ? globalThis.Number(object.endTime)
        : isSet(object.end_time)
        ? globalThis.Number(object.end_time)
        : 0,
      erthForBidders: isSet(object.erthForBidders)
        ? Coin.fromJSON(object.erthForBidders)
        : isSet(object.erth_for_bidders)
        ? Coin.fromJSON(object.erth_for_bidders)
        : undefined,
      erthForPool: isSet(object.erthForPool)
        ? Coin.fromJSON(object.erthForPool)
        : isSet(object.erth_for_pool)
        ? Coin.fromJSON(object.erth_for_pool)
        : undefined,
      totalRaised: isSet(object.totalRaised)
        ? globalThis.String(object.totalRaised)
        : isSet(object.total_raised)
        ? globalThis.String(object.total_raised)
        : "",
      poolId: isSet(object.poolId)
        ? globalThis.Number(object.poolId)
        : isSet(object.pool_id)
        ? globalThis.Number(object.pool_id)
        : 0,
      claimed: isSet(object.claimed) ? globalThis.String(object.claimed) : "",
    };
  },

  toJSON(message: LiquidityAuction): unknown {
    const obj: any = {};
    if (message.status !== 0) {
      obj.status = auctionStatusToJSON(message.status);
    }
    if (message.bidDenom !== "") {
      obj.bidDenom = message.bidDenom;
    }
    if (message.endTime !== 0) {
      obj.endTime = Math.round(message.endTime);
    }
    if (message.erthForBidders !== undefined) {
      obj.erthForBidders = Coin.toJSON(message.erthForBidders);
    }
    if (message.erthForPool !== undefined) {
      obj.erthForPool = Coin.toJSON(message.erthForPool);
    }
    if (message.totalRaised !== "") {
      obj.totalRaised = message.totalRaised;
    }
    if (message.poolId !== 0) {
      obj.poolId = Math.round(message.poolId);
    }
    if (message.claimed !== "") {
      obj.claimed = message.claimed;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<LiquidityAuction>, I>>(base?: I): LiquidityAuction {
    return LiquidityAuction.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<LiquidityAuction>, I>>(object: I): LiquidityAuction {
    const message = createBaseLiquidityAuction();
    message.status = object.status ?? 0;
    message.bidDenom = object.bidDenom ?? "";
    message.endTime = object.endTime ?? 0;
    message.erthForBidders = (object.erthForBidders !== undefined && object.erthForBidders !== null)
      ? Coin.fromPartial(object.erthForBidders)
      : undefined;
    message.erthForPool = (object.erthForPool !== undefined && object.erthForPool !== null)
      ? Coin.fromPartial(object.erthForPool)
      : undefined;
    message.totalRaised = object.totalRaised ?? "";
    message.poolId = object.poolId ?? 0;
    message.claimed = object.claimed ?? "";
    return message;
  },
};

function createBaseAuctionBid(): AuctionBid {
  return { bidder: "", amount: "", claimed: false };
}

export const AuctionBid: MessageFns<AuctionBid> = {
  encode(message: AuctionBid, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.bidder !== "") {
      writer.uint32(10).string(message.bidder);
    }
    if (message.amount !== "") {
      writer.uint32(18).string(message.amount);
    }
    if (message.claimed !== false) {
      writer.uint32(24).bool(message.claimed);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): AuctionBid {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const previousRecursionDepth = (reader as any).__tsProtoDecodeDepth ?? 0;
    if (previousRecursionDepth >= 100) {
      throw new globalThis.Error("protobuf decode recursion limit exceeded");
    }
    (reader as any).__tsProtoDecodeDepth = previousRecursionDepth + 1;
    try {
      const end = length === undefined ? reader.len : reader.pos + length;
      const message = createBaseAuctionBid();
      while (reader.pos < end) {
        const tag = reader.uint32();
        switch (tag >>> 3) {
          case 1: {
            if (tag !== 10) {
              break;
            }

            message.bidder = reader.string();
            continue;
          }
          case 2: {
            if (tag !== 18) {
              break;
            }

            message.amount = reader.string();
            continue;
          }
          case 3: {
            if (tag !== 24) {
              break;
            }

            message.claimed = reader.bool();
            continue;
          }
        }
        if ((tag & 7) === 4 || tag === 0) {
          break;
        }
        reader.skip(tag & 7);
      }
      return message;
    } finally {
      (reader as any).__tsProtoDecodeDepth = previousRecursionDepth;
    }
  },

  fromJSON(object: any): AuctionBid {
    return {
      bidder: isSet(object.bidder) ? globalThis.String(object.bidder) : "",
      amount: isSet(object.amount) ? globalThis.String(object.amount) : "",
      claimed: isSet(object.claimed) ? globalThis.Boolean(object.claimed) : false,
    };
  },

  toJSON(message: AuctionBid): unknown {
    const obj: any = {};
    if (message.bidder !== "") {
      obj.bidder = message.bidder;
    }
    if (message.amount !== "") {
      obj.amount = message.amount;
    }
    if (message.claimed !== false) {
      obj.claimed = message.claimed;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<AuctionBid>, I>>(base?: I): AuctionBid {
    return AuctionBid.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<AuctionBid>, I>>(object: I): AuctionBid {
    const message = createBaseAuctionBid();
    message.bidder = object.bidder ?? "";
    message.amount = object.amount ?? "";
    message.claimed = object.claimed ?? false;
    return message;
  },
};

type Builtin = Date | Function | Uint8Array | string | number | boolean | undefined;

export type DeepPartial<T> = T extends Builtin ? T
  : T extends globalThis.Array<infer U> ? globalThis.Array<DeepPartial<U>>
  : T extends ReadonlyArray<infer U> ? ReadonlyArray<DeepPartial<U>>
  : T extends {} ? { [K in keyof T]?: DeepPartial<T[K]> }
  : Partial<T>;

type KeysOfUnion<T> = T extends T ? keyof T : never;
export type Exact<P, I extends P> = P extends Builtin ? P
  : P & { [K in keyof P]: Exact<P[K], I[K]> } & { [K in Exclude<keyof I, KeysOfUnion<P>>]: never };

function longToNumber(int64: { toString(): string }): number {
  const num = globalThis.Number(int64.toString());
  if (num > globalThis.Number.MAX_SAFE_INTEGER) {
    throw new globalThis.Error("Value is larger than Number.MAX_SAFE_INTEGER");
  }
  if (num < globalThis.Number.MIN_SAFE_INTEGER) {
    throw new globalThis.Error("Value is smaller than Number.MIN_SAFE_INTEGER");
  }
  return num;
}

function isSet(value: any): boolean {
  return value !== null && value !== undefined;
}

export interface MessageFns<T> {
  encode(message: T, writer?: BinaryWriter): BinaryWriter;
  decode(input: BinaryReader | Uint8Array, length?: number): T;
  fromJSON(object: any): T;
  toJSON(message: T): unknown;
  create<I extends Exact<DeepPartial<T>, I>>(base?: I): T;
  fromPartial<I extends Exact<DeepPartial<T>, I>>(object: I): T;
}
