// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v2.12.0
//   protoc               unknown
// source: earth/dex/v1/unbonding.proto

/* eslint-disable */
import { BinaryReader, BinaryWriter } from "@bufbuild/protobuf/wire";
import { Coin } from "../../../cosmos/base/v1beta1/coin";

export const protobufPackage = "earth.dex.v1";

/**
 * LpUnbonding is an in-flight liquidity withdrawal.
 *
 * The shares are escrowed on the module account rather than burned, so they stay
 * part of the outstanding supply for the whole unbonding period and the pool
 * keeps trading on the liquidity behind them — otherwise the wait would cost the
 * provider time without buying the pool any depth.
 *
 * Because that liquidity is still working, the position keeps everything it
 * would have earned by staying: LP rewards, swap fees, and full exposure to the
 * pool. The price of leaving is time value alone — the capital is locked and at
 * risk for the unbonding period. The payout is therefore priced at maturity
 * rather than when unbonding began.
 */
export interface LpUnbonding {
  /** address is the provider the matured liquidity is swept to. */
  address: string;
  poolId: number;
  /** shares is the escrowed LP share coin awaiting maturity. */
  shares:
    | Coin
    | undefined;
  /**
   * completion_time is the unix timestamp (seconds) at which the sweep may pay
   * this entry out.
   */
  completionTime: number;
}

function createBaseLpUnbonding(): LpUnbonding {
  return { address: "", poolId: 0, shares: undefined, completionTime: 0 };
}

export const LpUnbonding: MessageFns<LpUnbonding> = {
  encode(message: LpUnbonding, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.address !== "") {
      writer.uint32(10).string(message.address);
    }
    if (message.poolId !== 0) {
      writer.uint32(16).uint64(message.poolId);
    }
    if (message.shares !== undefined) {
      Coin.encode(message.shares, writer.uint32(26).fork()).join();
    }
    if (message.completionTime !== 0) {
      writer.uint32(32).int64(message.completionTime);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): LpUnbonding {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseLpUnbonding();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.address = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 16) {
            break;
          }

          message.poolId = longToNumber(reader.uint64());
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.shares = Coin.decode(reader, reader.uint32());
          continue;
        }
        case 4: {
          if (tag !== 32) {
            break;
          }

          message.completionTime = longToNumber(reader.int64());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): LpUnbonding {
    return {
      address: isSet(object.address) ? globalThis.String(object.address) : "",
      poolId: isSet(object.poolId)
        ? globalThis.Number(object.poolId)
        : isSet(object.pool_id)
        ? globalThis.Number(object.pool_id)
        : 0,
      shares: isSet(object.shares) ? Coin.fromJSON(object.shares) : undefined,
      completionTime: isSet(object.completionTime)
        ? globalThis.Number(object.completionTime)
        : isSet(object.completion_time)
        ? globalThis.Number(object.completion_time)
        : 0,
    };
  },

  toJSON(message: LpUnbonding): unknown {
    const obj: any = {};
    if (message.address !== "") {
      obj.address = message.address;
    }
    if (message.poolId !== 0) {
      obj.poolId = Math.round(message.poolId);
    }
    if (message.shares !== undefined) {
      obj.shares = Coin.toJSON(message.shares);
    }
    if (message.completionTime !== 0) {
      obj.completionTime = Math.round(message.completionTime);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<LpUnbonding>, I>>(base?: I): LpUnbonding {
    return LpUnbonding.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<LpUnbonding>, I>>(object: I): LpUnbonding {
    const message = createBaseLpUnbonding();
    message.address = object.address ?? "";
    message.poolId = object.poolId ?? 0;
    message.shares = (object.shares !== undefined && object.shares !== null)
      ? Coin.fromPartial(object.shares)
      : undefined;
    message.completionTime = object.completionTime ?? 0;
    return message;
  },
};

type Builtin = Date | Function | Uint8Array | string | number | boolean | undefined;

export type DeepPartial<T> = T extends Builtin ? T
  : T extends globalThis.Array<infer U> ? globalThis.Array<DeepPartial<U>>
  : T extends ReadonlyArray<infer U> ? ReadonlyArray<DeepPartial<U>>
  : T extends {} ? { [K in keyof T]?: DeepPartial<T[K]> }
  : Partial<T>;

type KeysOfUnion<T> = T extends T ? keyof T : never;
export type Exact<P, I extends P> = P extends Builtin ? P
  : P & { [K in keyof P]: Exact<P[K], I[K]> } & { [K in Exclude<keyof I, KeysOfUnion<P>>]: never };

function longToNumber(int64: { toString(): string }): number {
  const num = globalThis.Number(int64.toString());
  if (num > globalThis.Number.MAX_SAFE_INTEGER) {
    throw new globalThis.Error("Value is larger than Number.MAX_SAFE_INTEGER");
  }
  if (num < globalThis.Number.MIN_SAFE_INTEGER) {
    throw new globalThis.Error("Value is smaller than Number.MIN_SAFE_INTEGER");
  }
  return num;
}

function isSet(value: any): boolean {
  return value !== null && value !== undefined;
}

export interface MessageFns<T> {
  encode(message: T, writer?: BinaryWriter): BinaryWriter;
  decode(input: BinaryReader | Uint8Array, length?: number): T;
  fromJSON(object: any): T;
  toJSON(message: T): unknown;
  create<I extends Exact<DeepPartial<T>, I>>(base?: I): T;
  fromPartial<I extends Exact<DeepPartial<T>, I>>(object: I): T;
}
